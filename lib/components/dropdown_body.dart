import 'package:flutter/material.dart';

class DropdownBody extends StatelessWidget {
  const DropdownBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
